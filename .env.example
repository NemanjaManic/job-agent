# Copy to .env for local testing (.env is in .gitignore, never committed).
# In production (GitHub Actions) these values come from GitHub Secrets, not this file.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
GEMINI_API_KEY=
